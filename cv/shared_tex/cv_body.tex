
%----------------------------------------------------------------------------------------
%
\begin{document}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
\ifthenelse{\boolean{isTwoColumnsStructure}}%
{%%% SPLIT CV INTO TWO COLUMNS
    \insertTwoColumnsStructure%
}
{%%% KEEP CV IN ONE COLUMN
    \begin{center} % Center everything in the document
        %
        \foreach \cvSection in \singleColumn {% iterate over section items to include them one by one
            \ifthenelse{\equal{\cvSection}{}}{}% check if is not empty
            {%
                % \textcolor{colorSuccess}{SECTION : \cvSection}% line for debugging
                \addContent{\cvSection}%
            }%
        }%
    \end{center}%
}%
%
\end{document}%